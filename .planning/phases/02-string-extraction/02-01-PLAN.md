---
phase: 02-string-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [I18N-07, I18N-08]
user_setup: []

must_haves:
  truths:
    - "All UI strings (400-600 keys) across codebase identified and cataloged"
    - "English strings extracted into feature-based JSON structure (common.json + 12 feature files)"
    - "Naming convention applied consistently (camelCase, semantic element-type organization)"
    - "All JSON files valid and loadable by i18next"
  artifacts:
    - path: "public/locales/en/common.json"
      provides: "Shared UI strings (buttons, generic labels, validation, pagination)"
      min_keys: 80
    - path: "public/locales/en/post.json"
      provides: "Post creation/editing feature strings"
      min_keys: 40
    - path: "public/locales/en/message.json"
      provides: "Messaging feature strings"
      min_keys: 40
    - path: "public/locales/en/community.json"
      provides: "Community management feature strings"
      min_keys: 30
    - path: "public/locales/en/auth.json"
      provides: "Authentication flow strings"
      min_keys: 25
    - path: "public/locales/en/dashboard.json"
      provides: "Dashboard/home feature strings"
      min_keys: 20
    - path: "public/locales/en/notifications.json"
      provides: "Notification strings"
      min_keys: 20
    - path: "public/locales/en/search.json"
      provides: "Search feature strings"
      min_keys: 15
    - path: "public/locales/en/profiles.json"
      provides: "User profile feature strings"
      min_keys: 20
    - path: "public/locales/en/users.json"
      provides: "User management strings"
      min_keys: 15
    - path: "public/locales/en/chatbot.json"
      provides: "Chatbot feature strings"
      min_keys: 10
    - path: "public/locales/en/settings.json"
      provides: "Settings feature strings"
      min_keys: 15
    - path: "public/locales/en/postComments.json"
      provides: "Post comments feature strings"
      min_keys: 25
  key_links:
    - from: "src/features/*"
      to: "public/locales/en/*.json"
      via: "String extraction from hardcoded UI text"
      pattern: "Every hardcoded string in features maps to a JSON key"
    - from: "public/locales/en/*.json"
      to: "i18next initialization"
      via: "Namespace configuration"
      pattern: "Each JSON file loadable by i18next"
---

<objective>
Identify and extract all hardcoded English UI strings from the codebase into feature-based JSON structure with semantic naming convention. Create the complete English translation inventory for Phase 3 component integration and Phase 5 Vietnamese translation.

**Purpose:** Establish the source of truth for all translatable UI strings, organized by feature and element type (action, label, error, etc.)

**Output:** 13 JSON files (common.json + 12 feature-specific files) with 500-700 total keys, all valid and loadable by i18next.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-string-extraction/02-CONTEXT.md
</execution_context>

<context>
**Phase 2 decisions (locked):**
- Namespace structure: Mixed approach (common.json + feature-specific files)
- Key naming: All camelCase (`featureName.elementType.property`)
- Element types: action, label, placeholder, error, validation, modal, help
- Scope: UI strings only (buttons, labels, form fields, errors, help text, modal titles/descriptions)

**Codebase context:**
- 339 TypeScript/TSX files to audit
- 12 feature folders with natural 1:1 mapping to JSON namespace files
- i18next already configured; `public/locales/en/` directory exists

**Requirements addressed:**
- I18N-07: Audit codebase for all UI strings
- I18N-08: Extract English strings into JSON files with feature-based structure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual audit of src/features/ — identify all UI strings by feature</name>
  <files>
    - Create: `02-AUDIT-LOG.md` (working document for audit progress)
  </files>
  <read_first>
    - src/features/auth/
    - src/features/posts/
    - src/features/messages/
    - src/features/communities/
    - src/features/dashboard/
    - src/features/notifications/
    - src/features/search/
    - src/features/profiles/
    - src/features/users/
    - src/features/chatbot/
    - src/features/settings/
    - src/features/post-comments/
    - .planning/phases/02-string-extraction/02-CONTEXT.md (naming conventions and structure)
  </read_first>
  <action>
Audit the codebase for all hardcoded English UI strings. Systematically scan each feature folder for:
- Button text (Create, Edit, Delete, Save, Cancel, Close, etc.)
- Form labels and placeholders
- Error messages and validation feedback
- Modal/dialog titles and descriptions
- Help text and tooltips
- Status messages
- Notification text
- Menu items and navigation labels

For each string found, record in a working audit log:
- String text (exact)
- File location (src/features/[feature]/[file].tsx)
- Component or context (where it's used)
- Element type (button, label, error, placeholder, modal title, etc.)
- Feature category (auth, posts, messages, communities, etc.)

**Approach:**
1. Start with src/features/auth/ — examine all .tsx files for hardcoded strings in JSX
2. For each component, identify strings in: button labels, form inputs/labels, error messages, modal content, help text
3. Move through each feature folder systematically (posts, messages, communities, dashboard, notifications, search, profiles, users, chatbot, settings, post-comments)
4. Also scan src/components/errors/, src/components/layouts/, and src/components/ui/ for reusable component strings
5. Record estimated counts per feature for scope validation

**Stop condition:** All 339 files have been scanned; comprehensive list of 400-600 strings captured in audit log.

**Deliverable:** `02-AUDIT-LOG.md` with complete list of strings organized by feature, including counts and element types.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-string-extraction/02-AUDIT-LOG.md" && grep -c "^-" ".planning/phases/02-string-extraction/02-AUDIT-LOG.md" | awk '{if ($1 >= 400) print "PASS: at least 400 strings identified"; else print "FAIL: fewer than 400 strings"}'</automated>
  </verify>
  <done>
- Audit log created and accessible
- All 12 feature folders have been scanned
- At least 400 unique UI strings identified
- Each string has: exact text, file location, element type, feature category
- Count per feature recorded for scope validation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create common.json with shared UI strings (buttons, labels, validation, pagination, errors)</name>
  <files>
    - Create: `public/locales/en/common.json`
  </files>
  <read_first>
    - .planning/phases/02-string-extraction/02-CONTEXT.md (common.json reserved strings and example structure)
    - .planning/phases/02-string-extraction/02-AUDIT-LOG.md (to identify which strings are truly common)
  </read_first>
  <action>
Create `public/locales/en/common.json` with truly shared UI elements that appear across multiple features. Use nested object structure organized by element type.

**String categories to include:**

1. **Buttons** (`button.*`)
   - `button.confirm`: "Confirm"
   - `button.cancel`: "Cancel"
   - `button.save`: "Save"
   - `button.delete`: "Delete"
   - `button.close`: "Close"
   - `button.back`: "Back"
   - `button.next`: "Next"
   - `button.submit`: "Submit"
   - `button.edit`: "Edit"
   - `button.view`: "View"
   - `button.create`: "Create"
   - `button.add`: "Add"

2. **Generic Labels** (`label.*`)
   - `label.loading`: "Loading..."
   - `label.noResults`: "No results found"
   - `label.emptyState`: "Nothing here yet"
   - `label.username`: "Username"
   - `label.email`: "Email"
   - `label.password`: "Password"

3. **Generic Errors** (`error.*`)
   - `error.networkError`: "Network error. Please try again."
   - `error.unauthorized`: "You don't have permission to do that."
   - `error.notFound`: "Not found."
   - `error.serverError`: "Server error. Please try again later."
   - `error.unknown`: "Something went wrong. Please try again."

4. **Validation Messages** (`validation.*`)
   - `validation.required`: "This field is required."
   - `validation.emailInvalid`: "Please enter a valid email address."
   - `validation.passwordTooWeak`: "Password must be at least 8 characters."
   - `validation.passwordMismatch`: "Passwords do not match."
   - `validation.minLength`: "Must be at least {count} characters."
   - `validation.maxLength`: "Must be no more than {count} characters."

5. **Pagination** (`pagination.*`)
   - `pagination.previous`: "Previous"
   - `pagination.next`: "Next"
   - `pagination.page`: "Page"
   - `pagination.of`: "of"

**Format (nested structure):**
```json
{
  "button": {
    "confirm": "Confirm",
    "cancel": "Cancel",
    ...
  },
  "label": {
    "loading": "Loading...",
    ...
  },
  "error": {
    "networkError": "Network error. Please try again.",
    ...
  },
  "validation": {
    "required": "This field is required.",
    ...
  },
  "pagination": {
    "previous": "Previous",
    ...
  }
}
```

**Rules:**
- Only include strings that appear in 2+ features
- If a feature has a custom variant (e.g., "Create Post" vs generic "Create"), keep the variant in the feature-specific file
- Use exact English text from audit log; verify against codebase
- Ensure valid JSON (test with `node -e "JSON.parse(require('fs').readFileSync('public/locales/en/common.json', 'utf8'));"`)
  </action>
  <verify>
    <automated>node -e "const json = JSON.parse(require('fs').readFileSync('public/locales/en/common.json', 'utf8')); const keys = Object.keys(json).length; console.log(keys >= 5 ? 'PASS: ' + keys + ' element types in common.json' : 'FAIL: fewer than 5 element types'); Object.values(json).forEach(v => typeof v === 'object' && Object.keys(v).forEach(k => { if (!v[k]) console.log('FAIL: empty value for ' + k); }));"</automated>
  </verify>
  <done>
- common.json created in public/locales/en/
- Valid JSON that parses without errors
- Contains 5+ element type categories (button, label, error, validation, pagination)
- Each category has 5+ string keys
- All strings are exact English text matching codebase usage
  </done>
</task>

<task type="auto">
  <name>Task 3: Create feature-specific JSON files (post.json, message.json, ... postComments.json)</name>
  <files>
    - Create: `public/locales/en/post.json`
    - Create: `public/locales/en/message.json`
    - Create: `public/locales/en/community.json`
    - Create: `public/locales/en/auth.json`
    - Create: `public/locales/en/dashboard.json`
    - Create: `public/locales/en/notifications.json`
    - Create: `public/locales/en/search.json`
    - Create: `public/locales/en/profiles.json`
    - Create: `public/locales/en/users.json`
    - Create: `public/locales/en/chatbot.json`
    - Create: `public/locales/en/settings.json`
    - Create: `public/locales/en/postComments.json`
  </files>
  <read_first>
    - .planning/phases/02-string-extraction/02-AUDIT-LOG.md (audit data for each feature)
    - .planning/phases/02-string-extraction/02-CONTEXT.md (naming conventions, element type organization)
    - src/features/[each-feature]/ (reference implementations for exact string text)
  </read_first>
  <action>
Create 12 feature-specific JSON files. Each file contains strings relevant to that feature, organized by semantic element type (action, label, placeholder, error, validation, modal, help).

**For each feature (auth, posts, messages, communities, dashboard, notifications, search, profiles, users, chatbot, settings, post-comments):**

1. Create `public/locales/en/[feature].json`
2. Populate with strings from audit log filtered by that feature
3. Organize by element type (action, label, placeholder, error, validation, modal, help)
4. Use exact English text from codebase (no paraphrasing)
5. Apply naming convention: `elementType.propertyName` (e.g., `action.create`, `modal.deleteConfirmation.title`)

**Example structure for post.json:**
```json
{
  "action": {
    "create": "Create Post",
    "edit": "Edit Post",
    "delete": "Delete Post",
    "share": "Share Post"
  },
  "label": {
    "title": "Post Title",
    "content": "Post Content",
    "author": "Posted by",
    "timestamp": "Posted on"
  },
  "placeholder": {
    "titleInput": "Enter post title...",
    "contentInput": "What's on your mind?"
  },
  "error": {
    "titleRequired": "Post title is required",
    "contentTooShort": "Post content must be at least 10 characters",
    "failedToCreate": "Failed to create post. Please try again."
  },
  "validation": {
    "pendingApproval": "Your post is pending community approval",
    "deletionPending": "Post deletion is being processed"
  },
  "modal": {
    "deleteConfirmation": {
      "title": "Delete Post?",
      "description": "This action cannot be undone.",
      "confirmButton": "Delete Permanently",
      "cancelButton": "Keep Post"
    },
    "editConfirmation": {
      "title": "Save Changes?",
      "confirmButton": "Save Changes",
      "cancelButton": "Discard"
    }
  },
  "help": {
    "postVisibility": "Choose who can see this post"
  }
}
```

**For each feature file:**
- Minimum 15-40 keys depending on feature size
- Post, Message, Community files: 40-50+ keys each
- Auth, Dashboard, Notifications, Search, Profiles: 20-40 keys each
- Chatbot, Settings, Users, PostComments: 10-25 keys each
- All JSON must be valid (no trailing commas, proper quoting)

**Verification per file:**
```bash
node -e "JSON.parse(require('fs').readFileSync('public/locales/en/[feature].json', 'utf8'));" 
```
  </action>
  <verify>
    <automated>for file in public/locales/en/{post,message,community,auth,dashboard,notifications,search,profiles,users,chatbot,settings,postComments}.json; do [ -f "$file" ] && node -e "JSON.parse(require('fs').readFileSync('$file', 'utf8'))" && echo "✓ $file valid" || echo "✗ $file missing or invalid"; done | grep -c "✓" | awk '{if ($1 == 12) print "PASS: all 12 feature files valid"; else print "FAIL: " (12 - $1) " files missing or invalid"}'</automated>
  </verify>
  <done>
- All 12 feature files created in public/locales/en/
- Each file contains valid JSON with no parse errors
- Each file has semantic organization by element type
- Total keys across all files: 300-600 (with common.json: 400-700 total)
- All strings are exact English text from codebase
- File naming matches feature names exactly (post.json, message.json, etc.)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Audit completeness:** `02-AUDIT-LOG.md` exists and contains 400+ unique strings
2. **JSON structure:** All 13 JSON files exist in `public/locales/en/`
3. **JSON validity:** Each file parses without errors
4. **Naming convention:** All keys follow camelCase pattern with semantic element-type organization
5. **String coverage:** No hardcoded English strings remain unencoded in extracted JSON
6. **Minimum key counts satisfied:** Common (80+), post/message/community (40+), auth/dashboard/notifications/search/profiles (20+), others (10+)
</verification>

<success_criteria>
Phase 2 Plan 01 succeeds when:

✅ `public/locales/en/common.json` exists with 80+ shared UI strings  
✅ `public/locales/en/{feature}.json` × 12 created with feature-specific strings (300-600 keys)  
✅ All 13 JSON files are valid and parseable by i18next  
✅ All strings use consistent camelCase naming convention (`featureName.elementType.property`)  
✅ Strings organized by semantic element type (action, label, error, validation, modal, help, placeholder)  
✅ Each string is exact English text from audit (not paraphrased)  
✅ Total inventory: 400-700 keys across all 13 files  
✅ Audit log preserved for translator reference (`02-AUDIT-LOG.md`)

No component integration yet (Phase 3). No Vietnamese translation yet (Phase 5).
</success_criteria>

<output>
After completion, create `.planning/phases/02-string-extraction/02-01-SUMMARY.md` documenting:
- Tasks executed (1-3)
- Files created (13 JSON files + audit log)
- Key counts per file and total
- Naming convention verification
- Dependencies satisfied (I18N-07, I18N-08)
</output>

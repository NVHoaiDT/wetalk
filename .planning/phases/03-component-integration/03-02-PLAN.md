---
phase: 03-component-integration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/features/auth/components/**, src/features/messages/components/**, src/features/posts/components/{create-post,edit-post,post-view,share-post,sort-posts}.tsx, src/app/router.tsx, src/testing/test-utils.tsx]
autonomous: true
requirements: [I18N-11, I18N-12, I18N-13, I18N-14]
must_haves:
  truths:
    - "All auth feature components (5 files) wired with useTranslation('auth') and t('key') calls"
    - "All message feature components (6 files) wired with useTranslation('messages') namespace"
    - "Select post components (5 critical files) wired: create-post, edit-post, post-view, share-post, sort-posts"
    - "Component test utils updated to include i18n provider wrapper for integration tests"
    - "Zero hardcoded English strings visible in auth, messages, posts UI (verified by visual scan or snapshot test)"
  artifacts:
    - path: "src/features/auth/components/login-form.tsx"
      provides: "Wired auth component using t('auth.*.key') pattern"
    - path: "src/features/auth/components/register-form.tsx"
      provides: "Wired auth component using t('auth.*.key') pattern"
    - path: "src/features/messages/components/message-input.tsx"
      provides: "Wired message component using t('message.*.key') pattern"
    - path: "src/testing/test-utils.tsx"
      provides: "Enhanced test utilities with i18n provider wrapper"
  key_links:
    - from: "src/features/auth/components/*.tsx"
      to: "public/locales/en/auth.json"
      via: "useTranslation('auth') hook + t('auth.*.key')"
      pattern: "const { t } = useTranslation"
    - from: "src/features/messages/components/*.tsx"
      to: "public/locales/en/message.json"
      via: "useTranslation('message') hook + t('message.*.key')"
      pattern: "const { t } = useTranslation"
---

<objective>
Wire highest-priority components with i18n integration. Complete auth feature (foundational), messages feature (core UX), and critical post components (feed-critical). Establish reusable wiring pattern for remaining features in Waves 3-4.

Purpose: Enable user authentication UI to work with i18n, core messaging feature to use translation keys, and critical post display/creation flows to be internationalized. Establish pattern templates for remaining 100+ components.

Output:
- Auth components fully wired (5 components, ~25 keys)
- Messages components fully wired (6 components, ~13 keys)
- Post critical path wired (5 components, ~15 keys)
- Enhanced test utilities with i18n wrapper
- Reusable wiring pattern documentation for Waves 3-4
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.planning/phases/02-string-extraction/02-AUDIT-DOCUMENT.md (reference keys and terminology)
@.planning/phases/03-component-integration/03-COMPONENT-AUDIT.md (component count and priority)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-component-integration/03-CONTEXT.md

## Translation Keys Available (from Phase 2)

**auth.json (18 keys):**
- action: login, register, forgotPassword, resetPassword, logout
- label: email, password, confirmPassword, rememberMe, or, terms, privacyPolicy
- error: invalidEmail, invalidPassword, userNotFound, userAlreadyExists, passwordTooShort, passwordMismatch
- placeholder: enterEmail, enterPassword
- validation: requiredField

**message.json (13 keys):**
- action: send, edit, delete, reply, archive
- label: newMessage, typing, online, offline, muted
- placeholder: typeMessage
- error: failedToSend, messageTooLong

**post.json (26 keys):**
- action: create, edit, delete, share, publish, save, report, upvote, downvote, follow
- label: title, content, author, timestamp, likes, comments, shares, saved
- placeholder: titleInput, contentInput, charCount
- error: titleRequired, contentTooShort, failedToCreate, failedToUpdate, failedToDelete
- validation: pendingApproval
- modal: [deleteConfirmation, shareToChat]
- help: postVisibility

## Team Distribution (Parallel Execution Possible)

**Engineer 1:** Auth feature (5 components, ~1-2 hours)
**Engineer 2:** Messages feature (6 components, ~1.5-2 hours)
**Engineer 3:** Critical posts (5 components, ~1.5-2 hours)
**All:** Test utilities update + pattern validation

Total Wave 2 effort: 4-6 hours for 16 components + 53 keys + test infrastructure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Auth Feature Components (5 files)</name>
  <files>src/features/auth/components/login-form.tsx, src/features/auth/components/register-form.tsx, src/features/auth/components/forgot-password-form.tsx, src/features/auth/components/reset-password-form.tsx, src/features/auth/components/login-google-form.tsx</files>
  <action>
Wire all 5 auth feature components with useTranslation('auth') hook and replace hardcoded strings with t() calls.

**Pattern to follow for all files:**

```typescript
import { useTranslation } from 'react-i18next';

export function LoginForm() {
  const { t } = useTranslation('auth');
  
  return (
    <form>
      <label>{t('label.email')}</label>
      <input placeholder={t('placeholder.enterEmail')} />
      <button>{t('action.login')}</button>
      <a href="/forgot">{t('action.forgotPassword')}</a>
    </form>
  );
}
```

**Files to update:**
1. login-form.tsx — Replace "Email", "Password", "Sign In", "Forgot Password" with t() calls
2. register-form.tsx — Replace "Email", "Password", "Confirm Password", "Create Account", "Terms", "Privacy" with t() calls
3. forgot-password-form.tsx — Replace "Reset Password", placeholder text with t() calls
4. reset-password-form.tsx — Replace "New Password", "Confirm", validation messages with t() calls
5. login-google-form.tsx — Replace "Continue with Google", any helper text with t() calls

**Implementation steps:**
- Add `import { useTranslation } from 'react-i18next';` to each file
- Extract existing hardcoded strings (grep for ['"][A-Z]) and record the exact text
- Map each hardcoded string to auth.json key (e.g., "Sign In" → t('action.login'))
- Replace all hardcoded string literals with t() calls
- Test each form manually (visual check: no broken labels, placeholders work, buttons display)

**Expected outcome:** All auth UI strings come from translation system, not hardcoded.
  </action>
  <verify>
    <automated>grep -r "useTranslation('auth')" src/features/auth/components/*.tsx | wc -l</automated>
  </verify>
  <done>
All 5 auth components wired: login-form.tsx, register-form.tsx, forgot-password-form.tsx, reset-password-form.tsx, login-google-form.tsx. Each imports and uses useTranslation('auth'). All ~25 auth keys from auth.json integrated (labels, actions, errors, placeholders, validation). Zero hardcoded English strings visible in auth UI. All form functions preserved (validation, submission, navigation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Messages Feature Components (6 files)</name>
  <files>src/features/messages/components/message-input.tsx, src/features/messages/components/message-item.tsx, src/features/messages/components/chat-panel.tsx, src/features/messages/components/conversations-panel.tsx, src/features/messages/components/conversation-item.tsx, src/features/messages/components/messages-popup.tsx</files>
  <action>
Wire all 6 message feature components with useTranslation('message') hook.

**Files to update:**
1. message-input.tsx — Replace "Type message", "Send", button text
2. message-item.tsx — Replace "Edit", "Delete", timestamps, status labels ("typing", "online", "offline")
3. chat-panel.tsx — Replace header labels, action buttons
4. conversations-panel.tsx — Replace "New Message", panel labels
5. conversation-item.tsx — Replace "Muted", "Active", and list labels
6. messages-popup.tsx — Replace "Messages", any header text

**Pattern (same as auth):**
```typescript
const { t } = useTranslation('message');
<input placeholder={t('placeholder.typeMessage')} />
<button>{t('action.send')}</button>
```

**Expected outcome:** Message UI fully internationalized, ready for Vietnamese in Phase 5.
  </action>
  <verify>
    <automated>grep -r "useTranslation('message')" src/features/messages/components/*.tsx | wc -l</automated>
  </verify>
  <done>
All 6 message components wired: message-input.tsx, message-item.tsx, chat-panel.tsx, conversations-panel.tsx, conversation-item.tsx, messages-popup.tsx. Each uses useTranslation('message'). All 13 message keys from message.json integrated (action, label, placeholder, error). Zero hardcoded English in message UI.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Critical Post Components (5 files) + Update Test Utils</name>
  <files>src/features/posts/components/create-post.tsx, src/features/posts/components/edit-post.tsx, src/features/posts/components/post-view.tsx, src/features/posts/components/share-post.tsx, src/features/posts/components/sort-posts.tsx, src/testing/test-utils.tsx</files>
  <action>
Wire 5 critical post components + update test utilities to include i18n provider wrapper.

**Post components to wire:**
1. create-post.tsx — Replace "Create Post", input labels, button text with t('post.action.*'), t('post.label.*')
2. edit-post.tsx — Replace "Edit Post", "Save", "Cancel" with t() calls
3. post-view.tsx — Replace "Like", "Comment", "Share", "Save" buttons and labels
4. share-post.tsx — Replace "Share", "To Chat", "Copy Link" with t() calls
5. sort-posts.tsx — Replace "Sort by", option labels with t() calls

**Also update src/testing/test-utils.tsx:**

Add i18n provider wrapper to test render functions:

```typescript
import { I18nextProvider } from 'react-i18next';
import i18n from '@/i18n/config';

export function renderWithProviders(
  ui: React.ReactElement,
  options?: RenderOptions
) {
  // Wrap components with both existing providers (Auth, etc.) AND i18n provider
  function Wrapper({ children }: { children: React.ReactNode }) {
    return (
      <I18nextProvider i18n={i18n}>
        {/* existing providers */}
        {children}
      </I18nextProvider>
    );
  }
  
  return render(ui, { wrapper: Wrapper, ...options });
}
```

This enables integration tests that verify i18n works in full component tree (not just unit tests with mocks).

**Expected outcome:** Post components wired, test utilities enhanced, any component test using custom render() now includes i18n provider.
  </action>
  <verify>
    <automated>grep -r "useTranslation('post')" src/features/posts/components/{create-post,edit-post,post-view,share-post,sort-posts}.tsx | wc -l</automated>
  </verify>
  <done>
5 critical post components wired (create-post.tsx, edit-post.tsx, post-view.tsx, share-post.tsx, sort-posts.tsx); each uses useTranslation('post'); ~15 post keys integrated. Test utilities (test-utils.tsx) updated: renderWithProviders() now wraps components with I18nextProvider, enabling integration tests with real i18n. Zero hardcoded English in critical post UI paths. Test suite ready for Wave 3+ components.
  </done>
</task>

</tasks>

<verification>
After completing Wave 2:
- ✓ Auth components fully wired (5 components, useTranslation verified)
- ✓ Message components fully wired (6 components, useTranslation verified)
- ✓ Critical post components wired (5 components)
- ✓ Test utilities enhanced with i18n provider wrapper
- ✓ Zero hardcoded English strings visible in auth/messages/posts UI
- ✓ Wiring pattern established and documented for remaining features
</verification>

<success_criteria>
- src/features/auth/components/*.tsx: All 5 files import useTranslation('auth') and use t() for all UI strings
- src/features/messages/components/*.tsx: All 6 files import useTranslation('message') [or 'messages'] and use t() for all UI strings
- src/features/posts/components/{create,edit,post-view,share,sort}*.tsx: All 5 files use useTranslation('post')
- src/testing/test-utils.tsx: renderWithProviders() includes I18nextProvider wrapper
- All 16 components pass visual inspection (no broken layout, no missing labels)
- Test suite continues to run without i18n-related failures (mocks work correctly)
</success_criteria>

<output>
After completion, create `.planning/phases/03-component-integration/03-02-SUMMARY.md` documenting Wave 2 completion (16 components + 53 keys wired + test infrastructure updated).
</output>

---
phase: 03-component-integration
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified: [src/features/**, src/components/**, src/features/post-comments/components/**, src/features/notifications/components/**, src/features/search/components/**, src/features/settings/components/**, src/features/users/components/**, src/features/chatbot/components/**, src/app/router.tsx]
autonomous: true
requirements: [I18N-11, I18N-12, I18N-13, I18N-14]
must_haves:
  truths:
    - "All post-comment components (8 files) wired with useTranslation('postComments')"
    - "All notification components (3 files) wired with useTranslation('notifications')"
    - "All search components (5 files) wired with useTranslation('search') "
    - "All settings components (3 files) wired with useTranslation('settings')"
    - "All UI primitives and layout components (40 files) wired with useTranslation('common')"
    - "All remaining feature components: users (1), chatbot (1) wired"
    - "Final verification: Scan entire codebase for remaining hardcoded English strings — report zero matches (or only in comments/external data)"
    - "Total: 140+ components wired with i18n, 186 translation keys in use"
  artifacts:
    - path: "src/features/post-comments/components/*.tsx"
      provides: "All post-comment components wired (8 files, 10 keys)"
    - path: "src/features/notifications/components/*.tsx"
      provides: "All notification components wired (3 files, 8 keys)"
    - path: "src/features/search/components/*.tsx"
      provides: "All search components wired (5 files, 6 keys)"
    - path: "src/features/settings/components/*.tsx"
      provides: "All settings components wired (3 files, 8 keys)"
    - path: "src/components/**/*.tsx"
      provides: "All UI primitives and layout components wired (40 files, 42 keys)"
    - path: ".planning/phases/03-component-integration/03-HARDCODED-SCAN.md"
      provides: "Final verification report: hardcoded English string scan results"
  key_links:
    - from: "src/** "
      to: "public/locales/en/"
      via: "useTranslation() hooks across all features"
      pattern: "const { t } = useTranslation"
---

<objective>
Complete wiring for all remaining features and UI primitives. Perform comprehensive final verification scan to confirm all hardcoded English strings have been replaced with translation keys. Achieve 100% component coverage (140+ components, 186 keys).

Purpose: Achieve full Phase 3 goal: Zero hardcoded English strings visible to users in any feature. Enable Phase 4 (language switching UI) and Phase 5 (Vietnamese translations) to proceed without discovery of forgotten components.

Output:
- Post-comments, notifications, search, settings fully wired (21 components, 32 keys)
- All UI primitives and layouts wired (40 components, 42 keys)
- All remaining minor features completed (2 components, 0 net new keys—use existing namespaces)
- Final hardcoded string scan: Zero matches (or only approved exceptions)
- 140+ components fully integrated with i18n
- Phase 3 goal achieved: Zero hardcoded English visible to users
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.planning/phases/02-string-extraction/02-AUDIT-DOCUMENT.md
@.planning/phases/03-component-integration/03-AUDIT.md
</execution_context>

<context>
@.planning/phases/03-component-integration/03-CONTEXT.md

## Remaining Translation Keys

**postComments.json (10 keys):**
- action: create, edit, delete, upvote, downvote, reply, report
- label: comment, timestamp
- error: failedToCreate

**notifications.json (8 keys):**
- action: markAsRead, markAllAsRead, clear, delete
- label: newNotification, noNotifications
- placeholder: searchNotifications

**search.json (6 keys):**
- action: search, filter, clear
- placeholder: searchQuery, searchUsers
- label: results

**settings.json (8 keys):**
- action: save, cancel, logout, deleteAccount
- label: accountSettings, notificationSettings, privacySettings
- error: saveSettings

**common.json (42 keys — used by UI primitives):**
- action: save, cancel, delete, submit, clear, previous, next, more, close, edit, create, loading
- label: required, optional, loading, error, success, warning, info, noData, empty, confirm, cancel
- error: [9 error types]
- validation: [5 validation types]
- pagination: [5 pagination terms]

## Components Remaining (Wave 4)

**Post-Comments (8):**
- create-post-comment, edit-post-comment, delete-post-comment, upvote-post-comment, downvote-post-comment, reply-comment, report-post-comment, post-comments-list

**Notifications (3):**
- notifications-list, notification-item, mark-notifications

**Search (5):**
- search, search-filters, search-posts-list, search-users-list, search-communities-list, search-all-list

**Settings (3):**
- setting-account, setting-notification, setting-preferences

**UI Primitives & Layouts (40):**
- Buttons, inputs, forms, dialogs, drawers, tables, grids, media viewers, editors, carousels
- Layouts: dashboard-layout, content-layout, auth-layout
- Components: link, dropdown, spinner, card, hover-card, notifications, select, SEO head

**Minor Features (2):**
- Users (user-hover-card)
- Chatbot (ai-chatbox)

Total Wave 4: ~60 components + final verification
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Smaller Features (21 files) — Post-Comments, Notifications, Search, Settings</name>
  <files>src/features/post-comments/components/**, src/features/notifications/components/**, src/features/search/components/**, src/features/settings/components/**</files>
  <action>
Wire all 21 components across 4 smaller features. Each uses corresponding namespace from Phase 2.

**Pattern (consistent across phase):**
```typescript
const { t } = useTranslation('featureName');
// Replace all hardcoded strings with t() calls
```

**Post-Comments (8 components):**
- create-post-comment, edit-post-comment, delete-post-comment, upvote-post-comment, downvote-post-comment, reply-comment, report-post-comment, post-comments-list
- Use useTranslation('postComments') + 10 keys from postComments.json
- Replace "Create Comment", "Edit", "Delete", "Reply", "Report" with t() calls

**Notifications (3 components):**
- notifications-list, notification-item, mark-notifications
- Use useTranslation('notifications') + 8 keys from notifications.json
- Replace "Notifications", "Mark as Read", "Clear", "No Notifications" with t() calls

**Search (5 components):**
- search, search-filters, search-posts-list, search-users-list, search-communities-list, search-all-list
- Use useTranslation('search') + 6 keys from search.json
- Replace "Search", filters, results labels with t() calls

**Settings (3 components):**
- setting-account, setting-notification, setting-preferences
- Use useTranslation('settings') + 8 keys from settings.json
- Replace "Account Settings", "Notification Settings", "Save", "Logout" with t() calls

**Expected outcome:** All 21 small-to-medium components fully wired. 32 keys distributed across 4 namespaces.
  </action>
  <verify>
    <automated>grep -r "useTranslation('" src/features/{post-comments,notifications,search,settings}/components/*.tsx | wc -l</automated>
  </verify>
  <done>
All 21 components across 4 features wired: post-comments (8), notifications (3), search (5), settings (3). Each feature's components import useTranslation with correct namespace (postComments, notifications, search, settings). All 32 keys distributed and integrated. Zero hardcoded English in any of these features.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire UI Primitives & Layouts (40 files)</name>
  <files>src/components/ui/**, src/components/layouts/**, src/components/seo/**, src/components/errors/**</files>
  <action>
Wire all 40 UI primitive and layout components with useTranslation('common'). These components use the 42 keys from common.json (shared across all features).

**UI Primitives categories:**
1. **Forms (10 files):** form, input, textarea, select, label, switch, error, field-wrapper, form-drawer
2. **Navigation & Interaction (5 files):** button, link, dropdown, drawer, dialog + confirmation-dialog
3. **Display (10 files):** card, spinner, hover-card, notifications, media-viewer, media-uploader, lightbox, link-preview, md-preview, message-media-viewer
4. **Tables & Data (5 files):** table, grid, pagination (table), pagination (grid)
5. **Layouts (3 files):** dashboard-layout, content-layout, auth-layout
6. **Other (2 files):** seo/head, errors/main

**Approach:**
- Forms: Wire input labels, error messages, validation text → common keys (label.required, error.*)
- Buttons: Wire action buttons → common keys (action.save, action.cancel, action.delete)
- Dialogs: Wire modal titles, confirmations → common keys (action.confirm, label.confirm)
- Tables: Wire pagination, empty states → common keys (pagination.*, label.empty, label.noData)
- Layouts: Wire generic structure labels → common keys (if any hardcoded text exists)

Use common.json's 42 keys to cover all shared UI elements. The 'common' namespace is designed to be reused across all features.

**Pattern:**
```typescript
// For UI component that shows a generic button
function Button({ label = 'action.save' }) {
  const { t } = useTranslation('common');
  return <button>{t(label)}</button>;
}
```

**Expected outcome:** All 40 UI and layout components use common namespace. Shared UI elements consistently translated via common.json.
  </action>
  <verify>
    <automated>grep -r "useTranslation('common')" src/components/**/*.tsx | wc -l</automated>
  </verify>
  <done>
All 40 UI primitive and layout components wired: forms (10), navigation (5), display (10), tables (5), layouts (3), other (2). Each uses useTranslation('common'). All 42 common keys integrated across UI layer. Zero hardcoded English in buttons, inputs, forms, dialogs, tables, or layouts. UI layer fully internationalized.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Remaining Minor Features (2 files) + Final Hardcoded String Scan</name>
  <files>src/features/users/components/user-hover-card.tsx, src/features/chatbot/components/ai-chatbox.tsx, .planning/phases/03-component-integration/03-HARDCODED-SCAN.md</files>
  <action>
Wire final 2 components and execute comprehensive final verification scan.

**Wire remaining components:**
1. **users/user-hover-card.tsx:** Wire with useTranslation('users') — or reuse common/profiles namespace if minimal strings. Replace "User", "Follow", "Message" if present.
2. **chatbot/ai-chatbox.tsx:** Wire with useTranslation('chatbot') — or reuse common namespace. Replace "Ask Chat", "Send", "Thinking" if present.

**Final Hardcoded String Scan:**

Create `.planning/phases/03-component-integration/03-HARDCODED-SCAN.md` documenting the final verification:

Execute comprehensive scan of entire src/ directory for remaining hardcoded English strings:

```bash
# Scan for potential hardcoded strings (capitalized English phrases in quotes)
grep -r "['\"]\s*[A-Z][a-zA-Z\s&:(){},.-]*['\"]\s*" src/components/ src/features/ \
  --exclude-dir=node_modules \
  --exclude="*.test.tsx" \
  --exclude="*.test.ts" \
  | grep -v "useTranslation" \
  | grep -v "placeholder=" \
  | grep -v "aria-label=" \
  | grep -v "title=" \
  | grep -v "alt=" \
  | grep -v "//" \
  | head -50
```

Document in HARDCODED-SCAN.md:
- Total matches found
- Breakdown by category (if any: comments, data constants, external text, etc.)
- Approved exceptions (e.g., API response text, user-generated content, comments)
- Result: "PASS — Zero hardcoded English strings found" or "REVIEW NEEDED — {N} matches found for human review"

**Expected outcome:** Final verification shows all hardcoded strings have been replaced with t() calls, OR remaining matches are documented as approved exceptions (not UI text visible to users).
  </action>
  <verify>
    <automated>test -f ".planning/phases/03-component-integration/03-HARDCODED-SCAN.md" && wc -l ".planning/phases/03-component-integration/03-HARDCODED-SCAN.md"</automated>
  </verify>
  <done>
Final 2 components wired (user-hover-card, ai-chatbox). Final hardcoded string scan executed and documented in 03-HARDCODED-SCAN.md. Report shows: {result}. Phase 3 goal achieved: 140+ components fully wired with useTranslation() hooks, 186 translation keys integrated, zero hardcoded English strings visible to users. All components ready for Phase 4 (language switching) and Phase 5 (Vietnamese translations).
  </done>
</task>

</tasks>

<verification>
After Wave 4 (Final):
- ✓ All post-comment components wired (8 files)
- ✓ All notification components wired (3 files)
- ✓ All search components wired (5 files)
- ✓ All settings components wired (3 files)
- ✓ All UI primitives and layouts wired (40 files)
- ✓ All minor features wired (2 files)
- ✓ Final hardcoded string scan completed (results documented)
- ✓ Total: 140+ components, 186 keys, zero hardcoded English
- ✓ Phase 3 goal achieved: All components integrated with i18n
</verification>

<success_criteria>
- All 21 smaller-feature components (post-comments, notifications, search, settings) pass import check: useTranslation present
- All 40 UI/layout components use useTranslation('common')
- Final 2 minor features (users, chatbot) wired
- src/components/ui/button/button.test.tsx passes (no i18n import errors)
- src/features/auth/components/login-form.test.tsx passes (mocking still functional)
- Hardcoded string scan completed; result documented in 03-HARDCODED-SCAN.md
- No visual regressions in any component
- App builds successfully: npm run build (Vite build check)
- All tests pass: npm test -- --run
</success_criteria>

<output>
After completion, create `.planning/phases/03-component-integration/03-04-SUMMARY.md` documenting Wave 4 completion (60+ final components + full hardcoded string verification). Also create `.planning/phases/03-component-integration/03-COMPLETION-REPORT.md` summarizing all Phase 3 work across 4 waves: 140+ components, 186 keys, zero hardcoded strings.
</output>
